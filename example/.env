PORT=8080
DOMAIN=localhost:8080

# Set to false to disable both /admin and /api/admin/*.
ADMIN_ENABLED=true

# Set a non-empty token to protect the admin interface.
ADMIN_TOKEN=your-token

SAVE_INTERVAL=30
MAX_BODY_SIZE=100MB
